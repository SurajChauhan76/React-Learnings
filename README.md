# 🚀 React Foundations & Enterprise Patterns

This repository documents my journey from mastering **React fundamentals** to applying **enterprise-level React engineering practices**.  
It serves as a structured guide for revision, stakeholder communication, and collaborative learning.

---

## React-Specific JavaScript Concepts

### 1. JSX
- Embedding expressions inside JSX
- Conditional rendering with ternaries and logical operators

### 2. Components
- Functional components (hooks-based)
- Class components (lifecycle methods)

### 3. Props & State
- Passing data via props
- Managing local state effectively

### 4. Hooks
- `useState` → local state management
- `useEffect` → side effects & lifecycle replacement
- `useContext` → global state sharing
- `useReducer` → complex state logic

### 5. Event Handling
- Synthetic events in React
- Binding event handlers

### 6. Forms
- Controlled components (state-driven inputs)
- Uncontrolled components (refs-driven inputs)

### 7. Lists & Keys
- Rendering arrays efficiently
- Importance of unique keys

### 8. Error Boundaries
- Catching runtime errors in components
- Fallback UI rendering

---

## Enterprise-Level React Patterns

### 1. State Management
- **Context API** for lightweight global state
- **Redux Toolkit** (actions, reducers, middleware)

### 2. Routing
- React Router basics
- Nested routes & protected routes

### 3. Performance Optimization
- Memoization (`React.memo`, `useMemo`, `useCallback`)
- Code splitting & lazy loading (`React.lazy`, `Suspense`)

### 4. Testing
- Unit & integration testing with **Jest**
- Component testing with **React Testing Library**

### 5. Type Safety
- TypeScript integration with React
- Typing props, state, and hooks

### 6. Styling
- CSS Modules
- Styled Components
- Tailwind CSS

### 7. Error Logging & Monitoring
- Sentry for error tracking
- LogRocket for session replay